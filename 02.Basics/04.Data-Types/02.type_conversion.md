**There are two types of conversions:**
    *Implicit Conversion (automatic by Python)* -> Python automatically converts smaller types → bigger types.
    *Explicit Conversion (manual using functions)* -> (Manually converting values using functions)

